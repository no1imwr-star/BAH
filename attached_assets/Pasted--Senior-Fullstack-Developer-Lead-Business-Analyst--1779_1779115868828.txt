Ты — Senior Fullstack Developer и Lead Business Analyst. Твоя цель — реализовать первую функциональную фичу для нашей CRM-системы автосервиса.

### КРИТИЧЕСКИЙ ЗАПРЕТ НА ОШИБКИ РЕНДЕРИНГА
Категорически запрещено использовать псевдографику, двойные рамки, разделительные линии из символов (════, ────, ----, ____) внутри любых блоков кода (особенно в ```mermaid или коде разметки). Это ломает парсер Replit и вызывает Lexical error. Все текстовые пояснения пиши строго ПОСЛЕ блоков кода обычным текстом.

### ЗАЩИТА ОТ ОШИБКИ RATE LIMIT (413 / TPM LIMIT)
Внимание! У нас жесткий лимит на количество токенов в минуту (6000 TPM). Чтобы не вызвать ошибку 413, тебе ЗАПРЕЩЕНО генерировать весь код для трех шагов ТЗ одновременно. 
Действуй по микро-шагам: сейчас напиши код ТОЛЬКО для Шага 1 (Схема данных). Не пиши логику подбора и эндпоинты, пока я не дам команду. В конце своего ответа просто напиши: «Шаг 1 готов. Напиши 'Продолжай', чтобы перейти к Шагу 2».

### СТАНДАРТЫ КАЧЕСТВА КОДА (LEAD BA / SENIOR)
1. Никакой «воды», пиши строгий, чистый и документированный код.
2. Обрабатывай краевые кейсы (edge cases), валидацию входящих данных и потенциальные ошибки.
3. Пиши код в соответствии со следующей архитектурной матрицей требований:
   - Бизнес-цель: Сократить время оформления клиента и автоматизировать подбор услуг.
   - SLA Производительности: Ответ эндпоинтов (Response time) не должен превышать 1.5 секунды.

### ТЕХНИЧЕСКОЕ ЗАДАНИЕ (User Story FR-01: Модуль подбора услуг)
Реализуй архитектуру, базу данных и API-эндпоинт для автоматического подбора услуг по параметрам автомобиля и текстовому описанию проблемы.

Выполни сейчас ТОЛЬКО Шаг 1:
Шаг 1. Схема данных (Database Schema)
Создай или обнови схему данных (используй стек проекта: SQL / Prisma / ORM). Мне нужны таблицы:
- `Cars` (id, brand, model, year)
- `Services` (id, name, price, duration, target_tags [] — массив тегов для поиска, например: "подвеска", "тормоза")
- `Orders` (id, car_id, customer_description, status)

Для контекста (реализуем на следующих шагах):
Шаг 2 будет включать бизнес-логику подбора (парсер текста по target_tags и фильтр по марке машины до ТОП-3 услуг).
Шаг 3 будет включать POST-эндпоинт `/api/orders/suggest-services` с обработкой ошибок 400 и 404.

Приступай к реализации Шага 1. Пиши код структурированно, без декоративных рамок, строго укладываясь в лимит токенов.
