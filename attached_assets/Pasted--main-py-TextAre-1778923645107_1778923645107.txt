Проведи глубокую модернизацию бэкенд-логики в main.py. Сделай текстовое поле ввода проблемы (TextArea) НЕОБЯЗАТЕЛЬНЫМ для пользователя. Сервис должен самостоятельно проводить математический анализ данных из загруженного Excel/CSV файла для генерации BPMN и Use Case.

Реализуй следующие изменения:

1. АВТОМАТИЧЕСКИЙ АНАЛИЗ ДАННЫХ НА PYTHON (Data Mining)
Перед отправкой запроса в Groq, добавь в функцию `/analyze` математический анализ датафрейма `df` с помощью pandas:
- Приведи названия колонок к нижнему регистру для удобства поиска.
- Найди колонку со стадиями сделок (ищи слова 'stage', 'стадия', 'статус'). Посчитай конверсию: процент сделок в статусе 'Closed Won' (или 'выиграна') от общего числа строк.
- Найди колонку со временем нахождения в этапе (ищи 'days', 'дни'). Вычисли среднее время (`.mean()`) для каждого этапа. Найди этап, где сделки виснут дольше всего (Бутылочное горлышко / Bottleneck).
- Найди колонку с причинами отказов (ищи 'reason', 'причина'). Посчитай топ-1 самую частую причину отказа (`.value_counts().idxmax()`).
- Сформируй из этих вычислений автоматическую строку проблемы:
  `auto_problem = f"Конверсия воронки составляет {conversion}%. Главное узкое место — этап '{bottleneck_stage}', где сделки зависают в среднем на {max_days} дней. Основная причина отказов — '{top_reason}'."`
- Если пользователь всё-таки ввел что-то руками в поле проблемы, объединяй его текст с `auto_problem`. Если поле пустое — используй только `auto_problem`.

2. ДИНАМИЧЕСКИЙ BPMN ШАБЛОН (На основе данных)
- Обнови логику f-строки для `BPMN_BASE_TEMPLATE`.
- Заставь ИИ через промпт вернуть строго определенные шаги для схемы на основе найденной проблемы.
- Твоя f-строка должна динамически подставлять в схему: Название начального этапа, Автоматический триггер CRM по оптимизации (например, "Авто-задача: Квалификация"), и Финальный статус ("Сделка выиграна"). Линии связи (`<bpmn:sequenceFlow>`) и графические стрелки (`<bpmndi:BPMNEdge>`) должны быть жестко сохранены в XML-шаблоне, чтобы схема не ломалась и не кривилась.

3. РЕФАКТОРИНГ СИСТЕМНОГО ПРОМПТА ДЛЯ GROQ
- Обнови промпт для модели "llama-3.1-8b-instant". 
- Инструкция: "Ты — Senior Business Analyst. Тебе передан автоматический отчет об анализе воронки продаж: {auto_problem}. На основе этих данных сгенерируй профессиональное ТЗ и Use Case по стандарту Коберна на русском языке. Сфокусируйся на оптимизации типового процесса квалификации лидов в CRM, улучшении автоматизации воронки и обновлении CRM-аналитики. Выдавай ответ строго через разделитель '|', где первыми элементами идут названия шагов для BPMN-схемы."

После изменения кода автоматически проверь его на синтаксические ошибки, сделай коммит и отправь (push) в GitHub репозиторий.
