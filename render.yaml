services:
  - type: web
    name: crm-process-optimizer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python artifacts/crm-optimizer/main.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: OPENAI_API_KEY
        sync: false
